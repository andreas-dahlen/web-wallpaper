{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:wallpaper": "node scripts/deploy-wallpaper.js",
    "deploy:debug-wallpaper": "node scripts/deploy-debug-wallpaper.js",
    "build:android": "npm run build && npm run deploy:wallpaper",
    "build:debug": "vite build --mode debug && npm run deploy:debug-wallpaper"
  },
  "dependencies": {
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.2.4",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
