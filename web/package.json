{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:android": "vite build --mode production && node scripts/deploy-wallpaper.js",
    "build:debug": "vite build --mode debug && node scripts/deploy-debug-wallpaper.js"
  },
  "dependencies": {
    "vue": "^3.5.24"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.2.4",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
